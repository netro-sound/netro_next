version: '3.9'

services:
  nextjs:
    build: .
    image: devsbaraus/netro_nextjs
    network_mode: host
    container_name: netro_nextjs
    restart: unless-stopped
    env_file:
      - .env.prod
    environment:
      - NODE_ENV=production
      - PORT=3333